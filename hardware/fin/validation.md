# K1 validation record

Package: K1.
Branch: `pkg/K1`.
Integration base: `16bb0ec`, still `origin/main` after the final fetch.
This is a manual record of the available checks, not a report generated by G1.

## Package acceptance

`python hardware/fin/build_fin.py` exited 0 using Python 3.13.15.
`python hardware/fin/build_fin.py --check` exited 0, including a run with `socket.socket` replaced by a function that raises.
The STL is one connected closed solid with 20 nondegenerate triangles, consistent outward orientation, a modelled bounding box of 150 × 41.5 × 5 mm, and a modelled volume of 3,525 mm³.
The SVG and the final PNG were visually inspected.
The OpenSCAD source was read, but OpenSCAD is not installed here and its renderer was not run.
No slicer, physical print, or structural simulation was run.
K1's full acceptance remains open until a person submits the print and runs the handling behaviour.

## Shared gate command

```sh
python tools/gates.py --package K1 --allowed "hardware/fin/,gates-log/K1.md,CHECKLIST.md" --append
```

Exit: 2, because `tools/gates.py` does not exist on this branch or its current main base.
The checklist explicitly allows K1's own acceptance checks while G1 is unavailable.
The command was attempted once, and must be rerun when G1 lands.
No fabricated runner output was written to `gates-log/`.

| Gate | Result | Evidence |
|---|---|---|
| F1 | NOT YET | G1's shared test runner is absent; K1's geometry check exits 0 |
| F2 | NOT YET | Shared S1/H2 checks await those packages; K1's check passes with sockets disabled |
| F3 | NOT YET | H2 page is absent |
| F4 | NOT YET | S1/H1 processed outputs are absent from this main base |
| F5 | NOT YET | H2/H3 labels are absent; CAD figures are labelled modelled |
| F6 | NOT YET | H1 is absent; K1's assumptions are documented locally |
| F7 | NOT YET | H1 is absent |
| F8 | NOT YET | H3 is absent |
| F9 | PASS | Manual diff against origin/main contains no planning changes |
| F10 | PASS | Only K1's checklist row and hardware/fin files changed |
| F11 | PASS | Tracked files and K1 artifacts checked for excluded paths and the 5 MB limit |
| F12 | NOT YET | S1 requirements are absent from this main base; K1 adds no Python dependency |
| F13 | PASS | No em dash found in Markdown outside planning and .claude |
| F14 | NOT YET | H1 crop sources are absent |

Regressions since the previous report: none identified in the available checks; the shared runner is unavailable.
Milestone gates due next: M1 after H1; M2, M3, and M4 remain open for the page, rehearsal, fallback, and submission.
Anything in the demo, the pitch, or a checklist that this package makes harder: the planned invitation to bend the fin still depends on printing and checking this PLA part; the CAD alone does not establish that behaviour.
