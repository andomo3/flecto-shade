# Gate reports for G1

## 1ec6ecd at 2026-09-19 23:27

```text
Package: G1   Commit: 1ec6ecd   Command: python tools/gates.py --package G1   Exit: 0

| Gate | Result  | Evidence                                                            |
| F1   | PASS    | 14 passed in 11.20s                                                 |
| F2   | NOT YET | no build command exists yet; S1 switches it on, then H2             |
| F3   | NOT YET | no .html, .css, or .js under software/ yet; H2 switches it on       |
| F4   | NOT YET | no software/tests/test_gate_f4_*.py yet; S1, then H1 switches it on |
| F5   | NOT YET | no software/tests/test_gate_f5_*.py yet; H2, then H3 switches it on |
| F6   | NOT YET | no software/tests/test_gate_f6_*.py yet; H1 switches it on          |
| F7   | NOT YET | no software/tests/test_gate_f7_*.py yet; H1 switches it on          |
| F8   | NOT YET | no software/tests/test_gate_f8_*.py yet; H3 switches it on          |
| F9   | PASS    | nothing changed under planning/                                     |
| F10  | PASS    | 3 changed files, all within: tools/, gates-log/, CHECKLIST.md       |
| F11  | PASS    | 115 tracked files, none private or over 5 MB                        |
| F12  | NOT YET | no software/requirements.txt yet; S1 switches it on                 |
| F13  | PASS    | no em dash in Markdown outside planning/ and .claude/               |
| F14  | NOT YET | no software/tests/test_gate_f14_*.py yet; H1 switches it on         |

Milestone gates M1 to M4 are checked by a person, see planning/plans/gates.md
Regressions since the last report: [fill in]
Milestone gates due next: [fill in]
Anything in the demo, the pitch, or a checklist that this package makes harder: [fill in]
```

## 8d0009d at 2026-09-20 00:52

```text
Package: G1   Commit: 8d0009d   Command: python tools/gates.py --package G1   Exit: 0

| Gate | Result  | Evidence                                                            |
| F1   | PASS    | 23 passed in 25.09s                                                 |
| F2   | NOT YET | no build command exists yet; S1 switches it on, then H2             |
| F3   | NOT YET | no .html, .css, or .js under software/ yet; H2 switches it on       |
| F4   | NOT YET | no software/tests/test_gate_f4_*.py yet; H1 switches it on          |
| F5   | NOT YET | no software/tests/test_gate_f5_*.py yet; H2, then H3 switches it on |
| F6   | NOT YET | no software/tests/test_gate_f6_*.py yet; H1 switches it on          |
| F7   | NOT YET | no software/tests/test_gate_f7_*.py yet; H1 switches it on          |
| F8   | NOT YET | no software/tests/test_gate_f8_*.py yet; H3 switches it on          |
| F9   | PASS    | nothing changed under planning/                                     |
| F10  | PASS    | 4 changed files, all within: tools/, gates-log/, CHECKLIST.md       |
| F11  | PASS    | 80 tracked files, none private or over 5 MB                         |
| F12  | NOT YET | no software/requirements.txt yet; S1 switches it on                 |
| F13  | PASS    | no em dash in Markdown outside planning/ and .claude/               |
| F14  | NOT YET | no software/tests/test_gate_f14_*.py yet; H1 switches it on         |

The year: NOT YET - no year test files yet; H1, then H3 switches them on (nonblocking)

Milestone gates M1 to M4 are checked by a person, see planning/plans/gates.md
Regressions since the last report: [fill in]
Milestone gates due next: [fill in]
Anything in the demo, the pitch, or a checklist that this package makes harder: [fill in]
```

## 380f28d at 2026-09-20 00:57

```text
Package: G1   Commit: 380f28d   Command: python tools/gates.py --package G1   Exit: 0

| Gate | Result  | Evidence                                                            |
| F1   | PASS    | 23 passed in 23.79s                                                 |
| F2   | NOT YET | no build command exists yet; S1 switches it on, then H2             |
| F3   | NOT YET | no .html, .css, or .js under software/ yet; H2 switches it on       |
| F4   | NOT YET | no software/tests/test_gate_f4_*.py yet; H1 switches it on          |
| F5   | NOT YET | no software/tests/test_gate_f5_*.py yet; H2, then H3 switches it on |
| F6   | NOT YET | no software/tests/test_gate_f6_*.py yet; H1 switches it on          |
| F7   | NOT YET | no software/tests/test_gate_f7_*.py yet; H1 switches it on          |
| F8   | NOT YET | no software/tests/test_gate_f8_*.py yet; H3 switches it on          |
| F9   | PASS    | nothing changed under planning/                                     |
| F10  | PASS    | 4 changed files, all within: tools/, gates-log/, CHECKLIST.md       |
| F11  | PASS    | 80 tracked files, none private or over 5 MB                         |
| F12  | NOT YET | no software/requirements.txt yet; S1 switches it on                 |
| F13  | PASS    | no em dash in Markdown outside planning/ and .claude/               |
| F14  | NOT YET | no software/tests/test_gate_f14_*.py yet; H1 switches it on         |

The year: NOT YET - no year test files yet; H1, then H3 switches them on (nonblocking)

Milestone gates M1 to M4 are checked by a person, see planning/plans/gates.md
Regressions since the last report: [fill in]
Milestone gates due next: [fill in]
Anything in the demo, the pitch, or a checklist that this package makes harder: [fill in]
```

### G1 handoff

- Regressions: none found in G1. All 23 tests pass. Ruff 0.13.2, `compileall`,
  `tabnanny`, and `git diff --check` pass. No project typechecker is configured.
- The command used above was
  `python tools/gates.py --package G1 --allowed "tools/,gates-log/,CHECKLIST.md" --append`.
  `CHECKLIST.md` is explicit because the package's own status row is part of this branch.
- F1 ignores only the two specified year files. Their results remain visible and
  never change the exit code. A failing ordinary test still blocks.
- F2 checks the present build commands without sockets and serves the static page
  over loopback when present. It does not run a browser or verify visual playback.
- Milestones next: human demo checks after H2 is accepted, then the result-card and
  pitch checks after H3. None is signed off by this report.
- What this package makes harder: nothing in the demo. Owners must name the year
  test files exactly as specified; until H1 separates them, its existing annual
  assertions still block F1.

### Integration check against current main's specification

Read-only source review and generated-output rebuilds in an isolated worktree.
No S1, H1, or H2 source, tests, or expected simulation values were changed.
Main remains untouched; Abba merges the packages.

| Reference | Revision |
|---|---|
| Current specification on main | `a220170` |
| S1 | `3674a08` |
| H1 | `39cf07a` |
| H2, which contains S1 and H1 | `b883bcf` (rechecked after its frontend update) |
| G1 runner used for the checks | `380f28d` |

Both public raw inputs were downloaded to ignored local directories and matched
the specification exactly:

- NASA POWER: `c81490d249cb1fc857b0edf44c54d6fcdbc7c1e5b30b326310138a10f25cefc4`.
- NOAA ISD: `d26366ed5b470b8a79d0c447b08b693b7b77f810bc8ee7a138c1a75682c64f08`.

Python 3.13.15 with pandas 2.2.3, numpy 2.2.3, and pytest 9.0.2 was used.
F2 ran `data/build_solar_2023.py`, `software/h1/build.py --city apopka`,
and `software/page/build_day.py --date 2023-06-03` with socket access refused.
All succeeded, and the static page returned HTTP 200 with its expected bytes.
The three package acceptance files passed with socket access refused on the initial
H2 snapshot `5c0a810`: **48 passed**. The final fetch brought H2's frontend update
`b883bcf`, and its refreshed gates also passed: **60 tests passed**, with offline
builds and local HTTP serving verified again.
The rebuilt tracked outputs have no content diff from the committed outputs.

The refreshed gates on the combined H2 worktree exited 0: F1 to F7 and F9,
F11 to F14 passed. F8 was NOT YET because H3 is absent. F10 was deliberately
NOT YET because this worktree contains several packages rather than a new package
with one allowed-path list. The year was NOT YET because neither named year file
exists. This does not mean the current specifications or the full demo pass.

#### Owner handoff before acceptance

1. **Ameya, H1:** move annual checks out of `software/tests/test_h1.py` into
   `software/tests/test_h1_year.py`, retaining their assertions and tolerances.
   Keep the demo and contract checks blocking. The current annual assertions pass
   here, but the missing split would let a future year mismatch block the demo.
2. **Frontend owner, H2:** make rain take priority in `play_seconds`, regenerate
   `day.json`, and update timing coverage to the current specification.
   The existing page test still asserts the superseded 38.6-second total.

   | Beat | Generated data now | Current specification |
   |---|---:|---:|
   | Fern closes | 15.0 s | 15.0 s |
   | Storm starts | 24.6 s | 24.6 s |
   | Storm duration | 4.8 s | 10.0 s |
   | Storm ends | 29.4 s | 34.6 s |
   | Playback ends | 38.6 s | 43.8 s |

   Both rainy hours, 15:00 and 16:00, currently use 2.4 seconds instead of 5.0.
   This removes the reading time reserved for the storm's three explanations in
   the pitch. Static review also found no distinct end-of-day table: `stop()`
   only changes the Play button. The required fallback layout is 4 rows of 6 fins
   per zone labelled "default layout"; the latest drawing uses 12 fins per zone
   and omits that label. These are H2 handoffs, not frontend changes in G1.
3. **Abba, H3 after H2 acceptance and merge:** implement the generated headline,
   identical page copy, result card, year view, and `pitch/figures.md`. None is
   present on the reviewed branches. Keep all pitch simulation figures provisional
   until that generator supplies them.
4. **Integrator:** run each package's acceptance and gates before merging in
   dependency order, then do the human demo and pitch checks, fallback recording,
   static deployment, and rehearsal. No browser run, deployment, or human sign-off
   is claimed here.

#### Windows checkout caveat

The first combined gate run gave 59 passed and one failure before F2 rebuilt the
data. A separate fresh worktree reproduced
`test_h1.py::test_two_runs_give_identical_bytes`: checked-out CSVs had CRLF endings,
while the builder writes LF. Rebuilding before tests, as the package acceptance
orders them, resolved it; subsequent builds and all 60 tests passed.
This was a line-ending mismatch, not a changed simulation result. Do not change
expected numeric values. The integrator can separately pin generated-file checkout
line endings if a clean-checkout gate run is required before any build.
